#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/python" ]; then
  "$basedir/python"  "$basedir/../youtube-dl/bin/youtube-dl" "$@"
  ret=$?
else 
  python  "$basedir/../youtube-dl/bin/youtube-dl" "$@"
  ret=$?
fi
exit $ret
